// Empty app skeleton. The UI was reset to a bare shell; pages and content will
// be rebuilt from here. For now there is nothing to mount beyond #app.
const root = document.getElementById('app');
void root;
